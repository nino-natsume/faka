# DCSHOP 多财商城系统 - 宝塔面板安装教程

### 学习版本以下内容无限制：
- 后台不再自动检查新版本（每次加载不再请求远程服务器）
- 侧边栏和程序坞不再显示"系统升级"菜单
- 首页版本状态显示"已是最新"而非"检查更新"按钮
- 不再弹出更新提示弹窗/强制更新跳转
- 不再显示插件/模板更新角标红点
- 后台管理功能（订单、商品、用户等）完全不受影响
- 不再连接远程授权服务器
- 所有插件/模板授权自动通过
- 后台不再显示"未授权"提示
- 系统标题不再添加"未授权的站点"前缀
- 保留原始的登录认证、权限控制、CSRF 等安全机制（不受影响）

## 宝塔环境要求（1panel类似）
推荐使用：宝塔 Linux 面板 + Nginx + PHP 7.4 + MySQL 5.7

1. 软件商店安装
   - Nginx 1.20+ 或 1.22+
   - PHP 7.4（推荐）
   - MySQL 5.7（MySQL 5.6 及以上也可）
   - phpMyAdmin（可选，用于管理数据库）

2.  PHP 必须开启的扩展 （一般在安装的PHP的时候自带这些扩展）
   进入：宝塔面板 → 软件商店 → PHP 7.4 → 设置 → 安装扩展
   请确认以下扩展已开启：
   - PDO
   - PDO_MySQL
   - cURL
   - JSON

3. PHP 推荐开启的扩展
   - mbstring
   - Zip
   - GD

## 宝塔面板安装DCSHOP详细步骤（1panel类似）

1. 创建网站
   进入：宝塔面板 → 网站 → 添加站点
   - 域名：填写你的正式域名，例如 xkwo.com
   - 根目录：保持宝塔默认或自定义目录均可
   - PHP版本：选择 PHP-74 +
   - 数据库：建议创建 MySQL 数据库
   - FTP：可按需创建，不是必须

2. 记录数据库信息
   添加站点时如果勾选了创建数据库，请记录：
   - 数据库名
   - 数据库用户名
   - 数据库密码

   如果没有创建数据库，可进入：
   宝塔面板 → 数据库 → 添加数据库
   手动创建一个新的 MySQL 数据库。

3. 上传程序文件
   进入：宝塔面板 → 文件 → 打开当前站点根目录
   将 DCSHOP 安装包上传到网站根目录并解压。

   注意：
   - 解压后必须让 index.php、install.php、admin/、content/ 等文件直接位于网站根目录。
   - 不要多套一层目录，例如不要变成 /www/wwwroot/域名/dcshop/index.php。
   - 如果多了一层目录，请把里面的全部文件移动到网站根目录。

4. 设置目录权限
   进入：宝塔面板 → 文件，确认以下目录/文件可写：
   - config.php
   - content/
   - content/plugins/
   - content/templates/
   - content/log/
   - content/cache/
   - content/uploadfile/

   如果安装时提示无法写入配置文件或缓存，可将上述目录权限设置为 755；
   个别服务器环境下仍不可写时，可临时设置为 777，安装完成后再按需收紧权限。

5. 配置 Nginx 伪静态（必做）
   进入：宝塔面板 → 网站 → 当前站点 → 设置 → 伪静态
   粘贴以下规则并保存：

location / {
    index index.php index.html;
    if (!-e $request_filename){
        rewrite ^/(.*)$ /index.php last;
    }
}

   说明：
   - 伪静态是必做项，不只是为了 SEO。
   - 支付成功跳转、异步回调、/action/return/、/action/notify/ 等路由都依赖该规则。
   - 如果不配置，支付成功返回地址可能会出现 404。
   - 后台如需开启 SEO 友好 URL，可在：后台 → 系统设置 → SEO设置 中配置。

6. 修改config.php文件里的数据库信息

   按页面引导填写：
   - 数据库主机：一般填写 localhost
   - 数据库名：填写宝塔创建的数据库名
   - 数据库用户名：填写宝塔创建的数据库用户名
   - 数据库密码：填写宝塔创建的数据库密码
   - 数据表前缀：默认 dc_ 即可
   - 管理员账号和密码
   - 站点地址

   然后点击“开始安装”。

7. 安装完成

   常用地址：
   - 前台地址：http://你的域名/
   - 后台地址：http://你的域名/admin/

   使用安装时设置的管理员账号密码登录后台。

## 常见问题

Q: 安装时提示“数据库连接失败”？

A: 请检查数据库名、数据库用户名、数据库密码是否正确。
   宝塔本机数据库主机通常填写 localhost。
   如果使用远程数据库，请填写远程数据库地址并确认已放行端口。


Q: 安装时提示“无法写入配置文件”？

A: 请检查网站根目录和 config.php 是否有写入权限。
   可在宝塔文件管理中给 config.php、content/ 及其子目录设置 755 或临时 777。


Q: 安装后页面空白或报错？

A: 请检查 PHP 版本是否为 7.4，并确认 PDO、PDO_MySQL、cURL、JSON 扩展已开启。


Q: 支付成功跳转 /action/return/xxx 显示 404？

A: 这是伪静态没有配置或没有生效导致的。
   请进入：宝塔面板 → 网站 → 当前站点 → 设置 → 伪静态
   粘贴本教程中的 Nginx 规则并保存，然后重新测试支付。


Q: 支付成功了，但订单没有自动发货？

A: 请先确认支付插件参数填写完整，其次确认伪静态已生效。
   支付平台的 notify_url 异步通知需要访问 /action/notify/插件名，
   如果伪静态未生效，通知地址会 404，订单就可能无法自动更新。


Q: 后台登录后授权页面一直显示“未授权”？

A: 点击后台授权页面的“刷新授权状态”，或清理系统缓存后重试。
   同时确认当前访问域名与授权绑定域名一致。


Q: 如何配置支付？

A: 后台 → 插件管理，启用需要的支付插件（如易支付、支付宝、码支付等），
   然后进入插件设置，填写对应的商户号、密钥、接口地址等参数。
   参数未配置完整时，对应支付方式不会在前台显示。


Q: 前台首页没有商品显示？

A: 后台 → 商品管理 → 添加商品，添加商品后前台即可显示。
   系统已预置测试商品，可根据实际业务修改或删除。


Q: 上传插件、模板或更新包失败？

A: 请检查 PHP 上传限制：
   宝塔面板 → 软件商店 → PHP 7.4 → 设置 → 配置修改
   调大 upload_max_filesize、post_max_size，并重启 PHP。
